#ifndef VOICE_USB_RPI_H
#define VOICE_USB_RPI_H

namespace tinylink {
        class Voice_USB_RPI {

        private:

        protected:

        public:
                Voice_USB_RPI();
                char* recognize(int time_s);
        };
}

extern tinylink::Voice_USB_RPI TL_Voice;

#endif

